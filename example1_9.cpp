/*
对字符数组进行操作
*/

#include <iostream>
#include <iterator>
#include <algorithm>
#include <functional>

using namespace std;

//void main() {
//	char a[] = "wearehere!", b[12];
//	reverse(a, a + 10);										//数组元素逆向
//	copy(a, a + 10, ostream_iterator< char >(cout));		//输出逆向后的元素
//	cout << endl;
//	copy(a, a + 12, b);										//a原样复制到数组b
//
//	sort(a, a + 10);										//默认升序排序
//	cout << a << endl;										//输出排序结果
//	cout << b << endl;										//输出数组b的内容
//	reverse_copy(a, a + 10, b);								//a逆向复制到数组b
//	cout << b << endl;
//
//	reverse(b + 2, b + 8);									//将数组b[2]-b[7]逆向
//	copy(b + 2, b + 8, ostream_iterator< char >(cout));		//输出b[2]-b[7]
//	cout << endl;
//
//	sort(a, a + 10, greater< char >());						//降幂排序
//	cout << a << endl;
//	cout << (*find(a, a + 10, 'h') == 'h') << " "
//		<< (*find(a, a + 10, 'o') == 'o') << endl;
//
//	system("pause");
//
//}


