/*
&是声明，不是定义
引用：就是将一个新标识符和一块已经存在的存储区域相关联
引用使用方法：
数据类型 & 别名 = 对象名
*/

#include <iostream>
using namespace std;

/*
void main() {
	int x = 56;					//定义并初始化x
	int &a = x;					//声明a是x的引用，a和x的地址相同
	int &r = a;					//声明r是a的引用，r和a的地址相同，即和x的地址相同
	cout << "x=" << x << ",&x=" << &x << ",a=" << a << ",&a=" << &a
		<< ",r=" << r << ",&r=" << &r << endl;
	r = 25;						//改变r，则改变a和x都同步变化
	cout << "x=" << x << ",&x=" << &x << ",a=" << a << ",&a=" << &a
		<< ",r=" << r << ",&r=" << &r << endl;
}
*/

